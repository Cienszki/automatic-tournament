import FantasyScoreCalculator from '@/components/fantasy/FantasyScoreCalculator';

export default function CalculatorPage() {
  return <FantasyScoreCalculator />;
}

export const metadata = {
  title: 'Fantasy Score Calculator | Tournament Tracker',
  description: 'Calculate fantasy scores manually using the exact tournament algorithm',
};